// The app's own tokens, as plain data (no React Native in that file).
export { SdsColors } from '@skkuverse/shared/src/tokens/colors';

/** The app's primary button (SDS theme `primary`). */
export const PRIMARY = '#1F3D2E';
