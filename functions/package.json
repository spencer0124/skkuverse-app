{
  "name": "functions",
  "private": true,
  "version": "0.0.1",
  "description": "Skkuverse Cloud Functions",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "lint": "ESLINT_USE_FLAT_CONFIG=false eslint --ext .ts src test scripts",
    "test": "node --experimental-strip-types --test --test-reporter=spec 'test/**/*.test.ts'",
    "backfill:prefs": "node --experimental-strip-types scripts/backfill-prefs.ts",
    "verify:trigger": "bash scripts/verify-trigger.sh",
    "verify:delete-account": "npm run build && JAVA_HOME=/opt/homebrew/opt/openjdk@25/libexec/openjdk.jdk/Contents/Home PATH=/opt/homebrew/opt/openjdk@25/libexec/openjdk.jdk/Contents/Home/bin:$PATH firebase emulators:exec --only functions,firestore,auth --project demo-skku-delete-account \"node --experimental-strip-types scripts/verify-delete-account.ts\""
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.0.1",
    "firebase-functions": "^7.2.5"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "@typescript-eslint/parser": "^8.15.0",
    "eslint": "^8.57.0"
  }
}
