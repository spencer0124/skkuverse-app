# Dependencies
node_modules/

# Expo
.expo/
dist/

# Native builds (CNG — regenerated by expo prebuild)
ios/
android/
!**/fastlane/metadata/android/
!**/fastlane/metadata/ios/
# Committed native source (the `ios/` rule above is unanchored — re-include hand-written
# native source for the Anemll local Expo module + app-target Swift, which live in ios/ dirs).
!apps/mobile/modules/**/ios/
!apps/mobile/modules/**/ios/**
!apps/mobile/native/ios/
!apps/mobile/native/ios/**

# Environment
.env
.env.local
.env.*.local
.envrc

# Firebase credentials
GoogleService-Info.plist
google-services.json

# App Store Connect API key
AuthKey_*.p8

# Build artifacts
build.ipa

# IDE
.vscode/settings.json
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build
*.tsbuildinfo

# Turborepo (future)
.turbo/

# Firebase CLI / Emulator
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
firestore-export/

# Claude Code — allow only workflow files
.github/*
!.github/workflows/
.github/workflows/*
!.github/workflows/claude-code-review.yml
!.github/workflows/claude.yml

# Claude Code local state (per-machine: scheduled-task locks, settings).
# Project-level CLAUDE.md and any docs/plans intentionally live at the
# repo root, NOT inside this directory.
.claude/
