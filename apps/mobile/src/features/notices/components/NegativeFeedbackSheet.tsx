/**
 * Re-export from the shared feedback module.
 * NegativeFeedbackSheet is no longer notices-specific — it lives in
 * src/features/feedback/ to support multiple surfaces.
 */
export { NegativeFeedbackSheet } from '@/features/feedback/NegativeFeedbackSheet';
