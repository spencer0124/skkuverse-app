/**
 * Re-export from the shared feedback module.
 * ReviewPromptSheet is no longer notices-specific — it is now generic and
 * lives in src/features/feedback/ to support multiple surfaces.
 */
export { ReviewPromptSheet } from '@/features/feedback/ReviewPromptSheet';
